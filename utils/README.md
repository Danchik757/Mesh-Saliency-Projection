# Utils

Use this folder for small reusable helpers:

- path helpers
- mesh IO helpers
- dataset column parsers
- visualization utilities that are shared by multiple methods

Current helper modules:

- [path_defaults.py](./path_defaults.py)
  - resolve external dataset roots from `--dataset-root` or environment variables
